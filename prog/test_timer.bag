#definition TIMER_IN (9232)
#definition TIMER_OUT (9233)

entier timerOut;

// Make the period of 1 sec (1 000 000 * 1 us)
ecrire_a(1, TIMER_IN);

tant_que (1 == 1)

	timerOut = lire_a(TIMER_IN);
	afficher_LCD(timerOut);

fin_tant_que;
